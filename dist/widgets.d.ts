// Code generated by tygo. DO NOT EDIT.

//////////
// source: decision_gate.go

/**
 * GateMode 是决策门的三态语义。
 */
export type GateMode = string;
export const GateModeConfirm: GateMode = "confirm";
export const GateModeInput: GateMode = "input";
export const GateModeChoice: GateMode = "choice";
/**
 * GateState 是决策门生命周期状态。
 */
export type GateState = string;
export const GateStatePending: GateState = "pending";
export const GateStateAnswered: GateState = "answered";
export const GateStateExpired: GateState = "expired";
/**
 * DecisionGate 是交互面的统一表达：一次能力运行中需要用户拍板的阻塞点。
 */
export interface DecisionGate {
  id: string;
  runId: string;
  canonicalName: string;
  mode: GateMode;
  prompt: string;
  options?: Option[];
  inputSchema?: InputSchema;
  preview?: PreviewBlock;
  sideEffectLevel?: string;
  state: GateState;
  answer?: GateAnswer;
  expiresAt?: number /* int64 */;
  createdAt: number /* int64 */;
  answeredAt?: number /* int64 */;
}
/**
 * Option 是 mode=choice 的一个候选。
 */
export interface Option {
  id: string;
  label: string;
  description?: string;
}
/**
 * InputSchema 是 mode=input 的补充输入契约。
 */
export interface InputSchema {
  fields: InputField[];
  allowSkip?: boolean;
}
/**
 * InputField 是一个补充输入字段。
 */
export interface InputField {
  name: string;
  kind: string;
  label: string;
  description?: string;
  required: boolean;
  options?: string[];
  allowFreeText?: boolean;
  accept?: string[];
  minFiles?: number /* int */;
  maxFiles?: number /* int */;
}
/**
 * GateAnswer 是回流后的用户拍板内容。
 */
export interface GateAnswer {
  chosenOptionId?: string;
  freeText?: string;
  inputPayload?: { [key: string]: unknown};
  confirmed?: boolean;
  source?: GateAnswerSource;
}
/**
 * GateAnswerSource 标记回流入口。
 */
export type GateAnswerSource = string;
export const GateAnswerSourceWarroom: GateAnswerSource = "warroom";
export const GateAnswerSourceChatText: GateAnswerSource = "chat_text";
export const GateAnswerSourceChatCard: GateAnswerSource = "chat_card";

//////////
// source: deliverable.go

/**
 * DeliverableType 是交付物语义类型，驱动前端卡片的图标、预览样式与默认动作。
 */
export type DeliverableType = string;
export const DeliverableArticle: DeliverableType = "article"; // 文章、推文、文案等长文本创作
export const DeliverableReport: DeliverableType = "report"; // 结构化报告
export const DeliverableSpreadsheet: DeliverableType = "spreadsheet"; // 表格数据
export const DeliverableCode: DeliverableType = "code"; // 代码、配置、脚本
export const DeliverableImageSet: DeliverableType = "image_set"; // 一组图片
export const DeliverableDocument: DeliverableType = "document"; // 文件型产物
export const DeliverableDataset: DeliverableType = "dataset"; // 数据集或批量结构化数据
export const DeliverableComposite: DeliverableType = "composite"; // 多个异质子产物聚合
/**
 * DeliverableState 是交付物生命周期。
 */
export type DeliverableState = string;
export const DeliverableStateDraft: DeliverableState = "draft"; // 内部过渡态，不回写主对话
export const DeliverableStateFinalized: DeliverableState = "finalized"; // 终态，回写主对话使用
export const DeliverableStateSuperseded: DeliverableState = "superseded"; // 预留给后续版本链
/**
 * PreviewKind 决定 PreviewBlock 哪个字段生效。
 */
export type PreviewKind = string;
export const PreviewKindMarkdown: PreviewKind = "markdown";
export const PreviewKindUIResource: PreviewKind = "ui_resource";
export const PreviewKindThumbnail: PreviewKind = "thumbnail";
/**
 * ArtifactKind 是附件、子产物、中间产物的资源种类。
 */
export type ArtifactKind = string;
export const ArtifactKindFile: ArtifactKind = "file";
export const ArtifactKindURL: ArtifactKind = "url";
export const ArtifactKindWidget: ArtifactKind = "widget";
export const ArtifactKindCode: ArtifactKind = "code";
export const ArtifactKindImage: ArtifactKind = "image";
export const ArtifactKindData: ArtifactKind = "data";
/**
 * ActionKind 是交付卡操作按钮种类。
 */
export type ActionKind = string;
export const ActionKindNavigate: ActionKind = "navigate";
export const ActionKindDownload: ActionKind = "download";
export const ActionKindTriggerCapability: ActionKind = "trigger_capability";
export const ActionKindExport: ActionKind = "export";
/**
 * Thumbnail 是 PreviewBlock(kind=thumbnail) 的单张缩略图。
 */
export interface Thumbnail {
  url: string;
  width?: number /* int */;
  height?: number /* int */;
  alt?: string;
}
/**
 * PreviewBlock 是主对话卡片主体和 DecisionGate dry-run 预览的共享展示块。
 */
export interface PreviewBlock {
  kind: PreviewKind;
  content?: string;
  ui_resource?: UIResource;
  thumbnails?: Thumbnail[];
  truncated?: boolean;
}
/**
 * ArtifactRef 是资源轻量引用，仅含定位信息。
 */
export interface ArtifactRef {
  kind: ArtifactKind;
  ref: string;
  title: string;
}
/**
 * Artifact 是交付物附件或子产物，可下载、可打开，也可带内联预览。
 */
export interface Artifact {
  kind: ArtifactKind;
  ref: string;
  title: string;
  preview?: PreviewBlock;
  mime?: string;
  size_bytes?: number /* int64 */;
}
/**
 * Action 是交付卡操作按钮，Payload 结构随 Kind 变化。
 */
export interface Action {
  label: string;
  kind: ActionKind;
  payload: unknown /* json.RawMessage */;
}
/**
 * DeliverableMetadata 是交付物时间戳与来源标识。
 */
export interface DeliverableMetadata {
  created_at: number /* int64 */;
  completed_at: number /* int64 */;
  source_kind?: string;
}
/**
 * Deliverable 是一次能力运行的最终产物结构化表达。
 */
export interface Deliverable {
  id: string;
  run_id: string;
  canonical_name: string;
  type: DeliverableType;
  title: string;
  summary: string;
  preview: PreviewBlock;
  artifacts?: Artifact[];
  actions?: Action[];
  state: DeliverableState;
  version: number /* int */;
  supersedes?: string;
  metadata: DeliverableMetadata;
}

//////////
// source: expert_activity.go

/**
 * ExpertActivity 是过程面里一个专家或编排官单 worker 的工作面快照。
 */
export interface ExpertActivity {
  expert_id: string;
  display_name: string;
  role?: string;
  avatar?: string;
  phase: ActivityPhase;
  status: ActivityStatus;
  current?: ActivityStep;
  steps?: ActivityStep[];
  drafts?: ArtifactRef[];
}
/**
 * ActivityStep 是工作面时间线的一条记录。
 */
export interface ActivityStep {
  kind: ActivityStepKind;
  text: string;
  done: boolean;
  ts: number /* int64 */;
}
/**
 * ActivityStepKind 区分动作、理由、消息和等待四类过程信息。
 */
export type ActivityStepKind = string;
export const StepKindAction: ActivityStepKind = "action";
export const StepKindReasoning: ActivityStepKind = "reasoning";
export const StepKindMessage: ActivityStepKind = "message";
export const StepKindWait: ActivityStepKind = "wait";
/**
 * ActivityPhase 是专家工作的粗粒度业务阶段。
 */
export type ActivityPhase = string;
export const PhaseResearch: ActivityPhase = "research";
export const PhaseDrafting: ActivityPhase = "drafting";
export const PhaseReviewing: ActivityPhase = "reviewing";
export const PhaseFinalizing: ActivityPhase = "finalizing";
export const PhaseDone: ActivityPhase = "done";
export const PhaseBlocked: ActivityPhase = "blocked";
/**
 * ActivityStatus 是专家当前运行态。
 */
export type ActivityStatus = string;
export const ActivityStatusRunning: ActivityStatus = "running";
export const ActivityStatusDone: ActivityStatus = "done";
export const ActivityStatusBlocked: ActivityStatus = "blocked";
export const ActivityStatusFailed: ActivityStatus = "failed";

//////////
// source: widget_postmessage.go

/**
 * postMessage 协议方法名（host ↔ iframe）。
 * 仅自定义 widget（ui:// scheme）路径用；共享 widget 走同源 React，不走 postmessage。
 * 协议层只定义方法名（wire format string），具体 payload 由前端 SDK 与 squad SDK 自行规范。
 */
export const PMMethodAppReady = "app.ready";
/**
 * postMessage 协议方法名（host ↔ iframe）。
 * 仅自定义 widget（ui:// scheme）路径用；共享 widget 走同源 React，不走 postmessage。
 * 协议层只定义方法名（wire format string），具体 payload 由前端 SDK 与 squad SDK 自行规范。
 */
export const PMMethodAppData = "app.data";
/**
 * postMessage 协议方法名（host ↔ iframe）。
 * 仅自定义 widget（ui:// scheme）路径用；共享 widget 走同源 React，不走 postmessage。
 * 协议层只定义方法名（wire format string），具体 payload 由前端 SDK 与 squad SDK 自行规范。
 */
export const PMMethodAppResize = "app.resize";
/**
 * postMessage 协议方法名（host ↔ iframe）。
 * 仅自定义 widget（ui:// scheme）路径用；共享 widget 走同源 React，不走 postmessage。
 * 协议层只定义方法名（wire format string），具体 payload 由前端 SDK 与 squad SDK 自行规范。
 */
export const PMMethodAppCallServerTool = "app.callServerTool";
/**
 * postMessage 协议方法名（host ↔ iframe）。
 * 仅自定义 widget（ui:// scheme）路径用；共享 widget 走同源 React，不走 postmessage。
 * 协议层只定义方法名（wire format string），具体 payload 由前端 SDK 与 squad SDK 自行规范。
 */
export const PMMethodAppToolResult = "app.toolResult";
/**
 * postMessage 协议方法名（host ↔ iframe）。
 * 仅自定义 widget（ui:// scheme）路径用；共享 widget 走同源 React，不走 postmessage。
 * 协议层只定义方法名（wire format string），具体 payload 由前端 SDK 与 squad SDK 自行规范。
 */
export const PMMethodAppToolError = "app.toolError";
/**
 * postMessage 协议方法名（host ↔ iframe）。
 * 仅自定义 widget（ui:// scheme）路径用；共享 widget 走同源 React，不走 postmessage。
 * 协议层只定义方法名（wire format string），具体 payload 由前端 SDK 与 squad SDK 自行规范。
 */
export const PMMethodAppUpdateModelContext = "app.updateModelContext";
/**
 * postMessage 协议方法名（host ↔ iframe）。
 * 仅自定义 widget（ui:// scheme）路径用；共享 widget 走同源 React，不走 postmessage。
 * 协议层只定义方法名（wire format string），具体 payload 由前端 SDK 与 squad SDK 自行规范。
 */
export const PMMethodAppClose = "app.close";
/**
 * postMessage 协议方法名（host ↔ iframe）。
 * 仅自定义 widget（ui:// scheme）路径用；共享 widget 走同源 React，不走 postmessage。
 * 协议层只定义方法名（wire format string），具体 payload 由前端 SDK 与 squad SDK 自行规范。
 */
export const PMMethodAppNotify = "app.notify";
/**
 * postMessage 协议方法名（host ↔ iframe）。
 * 仅自定义 widget（ui:// scheme）路径用；共享 widget 走同源 React，不走 postmessage。
 * 协议层只定义方法名（wire format string），具体 payload 由前端 SDK 与 squad SDK 自行规范。
 */
export const PMMethodAppOpenLink = "app.openLink";
/**
 * iframe sandbox flag 常量（自定义 widget 容器用）。
 * 默认基础集（不可变）：allow-scripts allow-forms
 * 故意去掉：allow-same-origin / allow-top-navigation / allow-modals / allow-popups
 * squad 在 ToolUIBinding.SandboxHints 申请追加 flag 时，必须落在本白名单内；
 * 平台侧 mount 阶段会校验。
 */
export const SandboxFlagAllowDownloads = "allow-downloads"; // keystone 默认在白名单
/**
 * iframe sandbox flag 常量（自定义 widget 容器用）。
 * 默认基础集（不可变）：allow-scripts allow-forms
 * 故意去掉：allow-same-origin / allow-top-navigation / allow-modals / allow-popups
 * squad 在 ToolUIBinding.SandboxHints 申请追加 flag 时，必须落在本白名单内；
 * 平台侧 mount 阶段会校验。
 */
export const SandboxFlagAllowPopups = "allow-popups"; // 默认不在
/**
 * iframe sandbox flag 常量（自定义 widget 容器用）。
 * 默认基础集（不可变）：allow-scripts allow-forms
 * 故意去掉：allow-same-origin / allow-top-navigation / allow-modals / allow-popups
 * squad 在 ToolUIBinding.SandboxHints 申请追加 flag 时，必须落在本白名单内；
 * 平台侧 mount 阶段会校验。
 */
export const SandboxFlagAllowModals = "allow-modals"; // 默认不在
/**
 * iframe sandbox flag 常量（自定义 widget 容器用）。
 * 默认基础集（不可变）：allow-scripts allow-forms
 * 故意去掉：allow-same-origin / allow-top-navigation / allow-modals / allow-popups
 * squad 在 ToolUIBinding.SandboxHints 申请追加 flag 时，必须落在本白名单内；
 * 平台侧 mount 阶段会校验。
 */
export const SandboxFlagAllowSameOrigin = "allow-same-origin"; // 默认严禁，特殊审批

//////////
// source: widget_uri.go

/**
 * WidgetURIScheme 是 widget URI 支持的两种 scheme：ks（共享 widget）/ ui（自定义 widget）。
 */
export type WidgetURIScheme = string;
/**
 * WidgetSchemeKS 表示共享 widget URI（ks://widgets/{name}@{vN}）。
 */
export const WidgetSchemeKS: WidgetURIScheme = "ks";
/**
 * WidgetSchemeCustom 表示自定义 widget URI（ui://{squad-id}/{path}）。
 */
export const WidgetSchemeCustom: WidgetURIScheme = "ui";
/**
 * WidgetURI 是 widget URI 解析后的结构。
 * 	ks 形态: scheme=ks, name=<widget-name>, version=<vN>
 * 	ui 形态: scheme=ui, name=<squad-id>/<path>, version=""
 */
export interface WidgetURI {
  Scheme: WidgetURIScheme;
  Name: string;
  Version: string;
  Raw: string;
}

//////////
// source: widgets.go
/*
Package kstypes widgets-protocol-v1：MCP tool widget binding 与 runtime UIResource 类型。

设计参考 docs/widgets-protocol-v1.md。
*/

/**
 * ToolUIBinding 是 squad 在 /meta.tools[]._meta.ui 里声明的 widget 绑定。
 * mount 时由平台侧入库。
 */
export interface ToolUIBinding {
  widget: string;
  sandbox_hints?: string[];
}
/**
 * UIResource 是 keystone proxy normalize 后输出的 runtime 字段，
 * 挂在 ToolCallResult.UIResource。前端 ToolUIRenderer 直接消费此结构。
 */
export interface UIResource {
  widget: string;
  data: unknown /* json.RawMessage */;
  sandbox_hints?: string[];
  src_url?: string;
  error?: UIResourceError;
}
/**
 * UIResourceError 描述 keystone 在 normalize 阶段对 widget 数据校验失败的诊断信息。
 */
export interface UIResourceError {
  code: string;
  message: string;
}
/**
 * MetaUIDecl 是 CallToolResult._meta.ui 的字段（squad runtime 响应里出现）。
 */
export interface MetaUIDecl {
  widget?: string;
  sandbox_hints?: string[];
}
/**
 * MetaKeystoneUIDecl 是 CallToolResult._meta.keystone.ui 的字段。
 */
export interface MetaKeystoneUIDecl {
  data: unknown /* json.RawMessage */;
}
/**
 * CapabilitiesUI 是 squad /meta.capabilities.ui 字段。
 */
export interface CapabilitiesUI {
  enabled: boolean;
  requested_sandbox?: string[];
}

//////////
// source: widgets_data.go

/**
 * WidgetActionDescriptor 描述 widget 上一个可点击 action（按钮）。
 * 渲染前端用 ActionButton 组件统一处理。
 */
export interface WidgetActionDescriptor {
  id: string;
  label: string;
  variant?: string; // "primary" | "default" | "destructive" | "ghost" | "link"
  icon?: string;
  disabled?: boolean;
  tooltip?: string;
  confirm_prompt?: string; // 非空时点击需二次确认
}
/**
 * WidgetBadge 是 widget 上的角标标签（如列表行的状态、卡片的 tag）。
 */
export interface WidgetBadge {
  label: string;
  variant?: string;
}
/**
 * WidgetEmptyState 是 widget 列表为空时的占位提示。
 */
export interface WidgetEmptyState {
  icon?: string;
  title: string;
  message?: string;
}
/**
 * WidgetListActionsV1 是 list-actions@v1 widget 的数据 schema。
 */
export interface WidgetListActionsV1 {
  title?: string;
  items: WidgetListItem[];
  actions?: WidgetActionDescriptor[];
  empty?: WidgetEmptyState;
}
/**
 * WidgetListItem 是列表里的单行条目。
 */
export interface WidgetListItem {
  id: string;
  title: string;
  subtitle?: string;
  icon?: string;
  badges?: WidgetBadge[];
  metadata?: { [key: string]: unknown};
  row_actions?: WidgetActionDescriptor[];
}
/**
 * WidgetDiffReviewV1 是 diff-review@v1 widget 的数据 schema。
 */
export interface WidgetDiffReviewV1 {
  title: string;
  subtitle?: string;
  diff: WidgetDiffSegment[];
  actions: WidgetActionDescriptor[];
  annotations?: WidgetDiffAnnotation[];
}
/**
 * WidgetDiffSegment 是一段 diff 文本。
 */
export interface WidgetDiffSegment {
  type: string; // "context" | "insert" | "delete"
  text: string;
}
/**
 * WidgetDiffAnnotation 是挂在 diff 某段上的批注（可选）。
 */
export interface WidgetDiffAnnotation {
  anchor_index: number /* int */;
  severity: string; // "info" | "warning" | "error"
  message: string;
}
/**
 * WidgetTimelineV1 是 timeline@v1 widget 的数据 schema。
 */
export interface WidgetTimelineV1 {
  title?: string;
  events: WidgetTimelineNode[];
}
/**
 * WidgetTimelineNode 是时间轴上的单个节点。
 * Time 字段必须为 RFC3339（ISO8601 UTC）格式。
 */
export interface WidgetTimelineNode {
  id: string;
  time: string; // ISO8601 UTC（RFC3339）
  title: string;
  status: string; // "pending" | "running" | "success" | "failed" | "skipped"
  subtitle?: string;
  detail?: string;
  actions?: WidgetActionDescriptor[];
}
/**
 * WidgetCardGridV1 是 card-grid@v1 widget 的数据 schema。
 * Columns 取值范围 [1,4]；0 视为未设置（前端默认 2 列）。
 */
export interface WidgetCardGridV1 {
  title?: string;
  columns?: number /* int */; // 默认 2，1-4
  cards: WidgetCard[];
}
/**
 * WidgetCard 是网格中的单张卡片。
 * SourceURL 必须为 http/https；Score 若设置必须在 [0,1] 区间。
 */
export interface WidgetCard {
  id: string;
  title: string;
  excerpt?: string;
  image_url?: string;
  source_label?: string;
  source_url?: string;
  score?: number /* float64 */;
  badges?: WidgetBadge[];
  actions?: WidgetActionDescriptor[];
}
/**
 * WidgetImageVariantsV1 是 image-variants@v1 widget 的数据 schema。
 */
export interface WidgetImageVariantsV1 {
  title?: string;
  primary: WidgetImageItem;
  variants?: WidgetImageItem[];
  actions?: WidgetActionDescriptor[];
}
/**
 * WidgetImageItem 是单张图片条目。
 * URL 必须为 https；AltText 必填；Width/Height 若非 0 必须 > 0。
 */
export interface WidgetImageItem {
  id: string;
  url: string; // https only
  alt_text: string;
  width?: number /* int */;
  height?: number /* int */;
  caption?: string;
  actions?: WidgetActionDescriptor[];
}
