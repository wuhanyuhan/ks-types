# ks-types v0.1.0 发布实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 补全 ks-types 4 项改进后发布 v0.1.0 tag，然后将 ks-admin 和 ks-hub 切换到正式版本。

**Architecture:** ks-types 是纯 Go library（module: `github.com/wuhanyuhan/ks-types`），被 ks-admin 和 ks-hub 通过 `replace => ../ks-types` 本地引用。本计划先完善 ks-types 代码质量，打 tag 发布到 GitHub，然后逐个切换下游项目到正式版本。

**Tech Stack:** Go 1.26.1, Ed25519/JWT, gin, sync.RWMutex

---

## 文件变更清单

| 操作 | 文件 | 职责 |
|------|------|------|
| 修改 | `errors.go` | 新增 `HTTPStatus()` 方法 |
| 修改 | `errors_test.go` | 新增 `TestBizErrorHTTPStatus` |
| 修改 | `permissions.go` | 加 `sync.RWMutex` 实现线程安全 |
| 修改 | `permissions_test.go` | 新增 `TestDefaultPermissionRegistry` + `TestPermissionRegistryConcurrency` |
| 修改 | `instance_claims_test.go` | 新增 `TestVerifyInstanceJWT_MalformedTokens` |
| 修改 | `developer_claims_test.go` | 新增 `TestVerifyDeveloperJWT_MalformedTokens` |
| 修改 | `../ks-admin/go.mod` | 删除 replace，锁定 v0.1.0 |
| 修改 | `../ks-hub/go.mod` | 删除 replace，锁定 v0.1.0 |

---

### Task 1: DefaultPermissionRegistry() 直接单元测试

**Files:**
- Modify: `permissions_test.go` (追加到文件末尾)

- [ ] **Step 1: 编写 TestDefaultPermissionRegistry 测试**

在 `permissions_test.go` 末尾追加：

```go
func TestDefaultPermissionRegistry(t *testing.T) {
	r := DefaultPermissionRegistry()
	if r == nil {
		t.Fatal("DefaultPermissionRegistry() returned nil")
	}

	// 验证预置的 4 个维度都已注册
	expectedDims := []string{"network", "llm", "filesystem", "user_context"}
	for _, key := range expectedDims {
		dim, ok := r.dimensions[key]
		if !ok {
			t.Errorf("missing dimension %q", key)
			continue
		}
		if len(dim.Levels) == 0 {
			t.Errorf("dimension %q has no levels", key)
		}
		if dim.RiskWeight <= 0 {
			t.Errorf("dimension %q: RiskWeight should be > 0, got %d", key, dim.RiskWeight)
		}
		if dim.DisplayName == "" {
			t.Errorf("dimension %q has empty DisplayName", key)
		}
	}

	// 用一个合法的权限声明验证 Validate 无 error
	perms := map[string]PermissionDecl{
		"network": {Level: "restricted"},
		"llm":     {Level: "host_proxy"},
	}
	warnings, err := r.Validate(perms)
	if err != nil {
		t.Errorf("Validate: unexpected error: %v", err)
	}
	if len(warnings) != 0 {
		t.Errorf("Validate: unexpected warnings: %v", warnings)
	}
}
```

- [ ] **Step 2: 运行测试确认通过**

```bash
go test -run TestDefaultPermissionRegistry -v ./...
```

预期输出：`PASS`

- [ ] **Step 3: 提交**

```bash
git add permissions_test.go
git commit -m "test: 补充 DefaultPermissionRegistry() 直接单元测试"
```

---

### Task 2: BizError HTTP 状态码映射

**Files:**
- Modify: `errors.go` (追加方法)
- Modify: `errors_test.go` (追加测试)

- [ ] **Step 1: 编写 TestBizErrorHTTPStatus 测试**

在 `errors_test.go` 末尾追加：

```go
func TestBizErrorHTTPStatus(t *testing.T) {
	cases := []struct {
		err        *BizError
		wantStatus int
	}{
		// 400xx → 400
		{ErrInvalidParams, 400},
		// 401xx → 401
		{ErrTokenExpired, 401},
		{ErrTokenInvalid, 401},
		{ErrInstanceInvalid, 401},
		{ErrInstanceRevoked, 401},
		// 403xx → 403
		{ErrForbidden, 403},
		// 404xx → 404
		{ErrNotFound, 404},
		{ErrAppNotFound, 404},
		{ErrVersionNotFound, 404},
		// 409xx → 409
		{ErrDuplicate, 409},
		{ErrManifestInvalid, 409},
		{ErrManifestIDMismatch, 409},
		{ErrChecksumMismatch, 409},
		{ErrPermissionInvalid, 409},
		{ErrPermissionUnknown, 409},
		// 500xx → 500
		{ErrInternalServer, 500},
		// 503xx → 503
		{ErrStorageUploadFailed, 503},
		{ErrStorageDownloadFailed, 503},
		// 未知码 → 500 兜底
		{&BizError{99999, "未知错误"}, 500},
	}
	for _, c := range cases {
		got := c.err.HTTPStatus()
		if got != c.wantStatus {
			t.Errorf("BizError{%d}.HTTPStatus() = %d, want %d", c.err.Code, got, c.wantStatus)
		}
	}
}
```

- [ ] **Step 2: 运行测试确认失败**

```bash
go test -run TestBizErrorHTTPStatus -v ./...
```

预期：编译失败，`BizError` 没有 `HTTPStatus` 方法。

- [ ] **Step 3: 实现 HTTPStatus 方法**

在 `errors.go` 末尾（`ErrInternalServer` 定义之后）追加：

```go
// HTTPStatus 根据错误码前缀推导 HTTP 状态码
func (e *BizError) HTTPStatus() int {
	prefix := e.Code / 100
	switch prefix {
	case 400:
		return 400
	case 401:
		return 401
	case 403:
		return 403
	case 404:
		return 404
	case 409:
		return 409
	case 500:
		return 500
	case 503:
		return 503
	default:
		return 500
	}
}
```

- [ ] **Step 4: 运行测试确认通过**

```bash
go test -run TestBizErrorHTTPStatus -v ./...
```

预期输出：`PASS`

- [ ] **Step 5: 提交**

```bash
git add errors.go errors_test.go
git commit -m "feat: BizError.HTTPStatus() 根据错误码前缀推导 HTTP 状态码"
```

---

### Task 3: PermissionRegistry 线程安全

**Files:**
- Modify: `permissions.go` (加锁)
- Modify: `permissions_test.go` (追加并发测试)

- [ ] **Step 1: 编写 TestPermissionRegistryConcurrency 测试**

在 `permissions_test.go` 末尾追加：

```go
func TestPermissionRegistryConcurrency(t *testing.T) {
	r := DefaultPermissionRegistry()

	done := make(chan struct{})
	// 并发写：注册新维度
	go func() {
		defer func() { done <- struct{}{} }()
		for i := 0; i < 100; i++ {
			r.Register(fmt.Sprintf("dim_%d", i), PermissionDimension{
				DisplayName: fmt.Sprintf("维度%d", i),
				Levels:      []string{"none", "full"},
				RiskWeight:  i,
			})
		}
	}()

	// 并发读：校验权限
	go func() {
		defer func() { done <- struct{}{} }()
		for i := 0; i < 100; i++ {
			perms := map[string]PermissionDecl{
				"network": {Level: "restricted"},
			}
			r.Validate(perms)
		}
	}()

	// 并发读：高风险检测
	go func() {
		defer func() { done <- struct{}{} }()
		for i := 0; i < 100; i++ {
			perms := map[string]PermissionDecl{
				"filesystem": {Level: "full"},
			}
			r.HighRiskPermissions(perms, 5)
		}
	}()

	<-done
	<-done
	<-done
}
```

注意：`permissions_test.go` 顶部的 import 需要添加 `"fmt"`。

- [ ] **Step 2: 运行 race 检测确认失败**

```bash
go test -race -run TestPermissionRegistryConcurrency -v ./...
```

预期：检测到 data race（`Register` 和 `Validate` 并发访问 `dimensions` map）。

- [ ] **Step 3: 给 PermissionRegistry 加锁**

修改 `permissions.go`：

1. import 中添加 `"sync"`

2. 结构体加 mutex：

```go
type PermissionRegistry struct {
	mu         sync.RWMutex
	dimensions map[string]PermissionDimension
}
```

3. `Register` 方法加写锁：

```go
func (r *PermissionRegistry) Register(key string, dim PermissionDimension) {
	r.mu.Lock()
	defer r.mu.Unlock()
	r.dimensions[key] = dim
}
```

4. `Validate` 方法加读锁：

```go
func (r *PermissionRegistry) Validate(perms map[string]PermissionDecl) ([]PermissionWarning, error) {
	r.mu.RLock()
	defer r.mu.RUnlock()

	var warnings []PermissionWarning
	// ... 后续逻辑不变
```

5. `HighRiskPermissions` 方法加读锁：

```go
func (r *PermissionRegistry) HighRiskPermissions(perms map[string]PermissionDecl, threshold int) []string {
	r.mu.RLock()
	defer r.mu.RUnlock()

	var result []string
	// ... 后续逻辑不变
```

- [ ] **Step 4: 运行 race 检测确认通过**

```bash
go test -race -run TestPermissionRegistryConcurrency -v ./...
```

预期输出：`PASS`（无 race）

- [ ] **Step 5: 运行全量测试确认无回归**

```bash
go test -race -v ./...
```

预期：全部通过。

注意：Task 1 中 `TestDefaultPermissionRegistry` 直接访问 `r.dimensions`（非导出字段），因为测试在同一个 package（`kstypes`）中，加锁不影响该测试。

- [ ] **Step 6: 提交**

```bash
git add permissions.go permissions_test.go
git commit -m "feat: PermissionRegistry 加 sync.RWMutex 保证线程安全"
```

---

### Task 4: 恶意 JWT token 边界测试

**Files:**
- Modify: `instance_claims_test.go` (追加测试)
- Modify: `developer_claims_test.go` (追加测试)

- [ ] **Step 1: 在 instance_claims_test.go 追加恶意 token 测试**

在 `instance_claims_test.go` 末尾追加：

```go
func TestVerifyInstanceJWT_MalformedTokens(t *testing.T) {
	_, pub := loadTestKeys(t)

	cases := []struct {
		name  string
		token string
	}{
		{"空字符串", ""},
		{"随机垃圾", "not-a-jwt-token-at-all"},
		{"只有 header", "eyJhbGciOiJFZERTQSIsInR5cCI6IkpXVCJ9"},
		{"两段（缺 signature）", "eyJhbGciOiJFZERTQSIsInR5cCI6IkpXVCJ9.eyJzdWIiOiJ0ZXN0In0"},
		{"三段但 signature 损坏", "eyJhbGciOiJFZERTQSIsInR5cCI6IkpXVCJ9.eyJzdWIiOiJ0ZXN0In0.AAAA_invalid_sig"},
	}

	for _, c := range cases {
		t.Run(c.name, func(t *testing.T) {
			_, err := VerifyInstanceJWT(c.token, pub)
			if err == nil {
				t.Errorf("expected error for malformed token %q", c.name)
			}
		})
	}
}
```

- [ ] **Step 2: 运行测试确认通过**

```bash
go test -run TestVerifyInstanceJWT_MalformedTokens -v ./...
```

预期：全部 PASS（jwt 库会拒绝这些畸形 token）。

- [ ] **Step 3: 在 developer_claims_test.go 追加恶意 token 测试**

在 `developer_claims_test.go` 末尾追加：

```go
func TestVerifyDeveloperJWT_MalformedTokens(t *testing.T) {
	pub, _ := os.ReadFile("testdata/test_public.pem")

	cases := []struct {
		name  string
		token string
	}{
		{"空字符串", ""},
		{"随机垃圾", "not-a-jwt-token-at-all"},
		{"只有 header", "eyJhbGciOiJFZERTQSIsInR5cCI6IkpXVCJ9"},
		{"两段（缺 signature）", "eyJhbGciOiJFZERTQSIsInR5cCI6IkpXVCJ9.eyJzdWIiOiJ0ZXN0In0"},
		{"三段但 signature 损坏", "eyJhbGciOiJFZERTQSIsInR5cCI6IkpXVCJ9.eyJzdWIiOiJ0ZXN0In0.AAAA_invalid_sig"},
	}

	for _, c := range cases {
		t.Run(c.name, func(t *testing.T) {
			_, err := VerifyDeveloperJWT(c.token, pub)
			if err == nil {
				t.Errorf("expected error for malformed token %q", c.name)
			}
		})
	}
}
```

- [ ] **Step 4: 运行测试确认通过**

```bash
go test -run TestVerifyDeveloperJWT_MalformedTokens -v ./...
```

预期：全部 PASS。

- [ ] **Step 5: 提交**

```bash
git add instance_claims_test.go developer_claims_test.go
git commit -m "test: 恶意/畸形 JWT token 边界测试"
```

---

### Task 5: 全量验证 + 静态检查

**Files:** 无修改（纯验证步骤）

- [ ] **Step 1: 运行全量测试**

```bash
cd /home/parallels/projects/yuhan/ks-types && go test -v ./...
```

预期：全部 PASS。

- [ ] **Step 2: 运行 race 检测**

```bash
go test -race ./...
```

预期：PASS，无 data race。

- [ ] **Step 3: 运行静态检查**

```bash
go vet ./...
```

预期：无输出（零问题）。

- [ ] **Step 4: 检查覆盖率**

```bash
go test -coverprofile=/tmp/ks-types-coverage.out ./... && go tool cover -func=/tmp/ks-types-coverage.out | tail -1
```

预期：总覆盖率 ≥ 85%（相比原先 82.7% 应有提升）。

---

### Task 6: 打 tag v0.1.0 并推送

**Files:** 无修改（git 操作）

- [ ] **Step 1: 确认工作区干净**

```bash
cd /home/parallels/projects/yuhan/ks-types && git status
```

预期：`nothing to commit, working tree clean`

- [ ] **Step 2: 推送 master 到远程**

```bash
git push origin master
```

- [ ] **Step 3: 打 tag**

```bash
git tag v0.1.0
```

- [ ] **Step 4: 推送 tag**

```bash
git push origin v0.1.0
```

- [ ] **Step 5: 验证 tag 存在**

```bash
git ls-remote --tags origin | grep v0.1.0
```

预期：输出包含 `refs/tags/v0.1.0`。

---

### Task 7: ks-admin 切换到 v0.1.0

**Files:**
- Modify: `../ks-admin/go.mod` (第 16 行删除 replace，第 10 行版本号会被 go get 更新)

- [ ] **Step 1: 删除 replace 指令**

编辑 `/home/parallels/projects/yuhan/ks-admin/go.mod`，删除第 16 行：

```
replace github.com/wuhanyuhan/ks-types => ../ks-types
```

- [ ] **Step 2: 获取正式版本**

```bash
cd /home/parallels/projects/yuhan/ks-admin && go get github.com/wuhanyuhan/ks-types@v0.1.0
```

- [ ] **Step 3: 整理依赖**

```bash
go mod tidy
```

- [ ] **Step 4: 运行测试验证兼容性**

```bash
go test ./...
```

预期：全部 PASS。

- [ ] **Step 5: 提交并推送**

```bash
git add go.mod go.sum && git commit -m "chore: 切换 ks-types 到 v0.1.0 正式版本"
git push origin master
```

---

### Task 8: ks-hub 切换到 v0.1.0

**Files:**
- Modify: `../ks-hub/go.mod` (第 92 行删除 replace，第 15 行版本号会被 go get 更新)

- [ ] **Step 1: 删除 replace 指令**

编辑 `/home/parallels/projects/yuhan/ks-hub/go.mod`，删除第 92 行：

```
replace github.com/wuhanyuhan/ks-types => ../ks-types
```

- [ ] **Step 2: 获取正式版本**

```bash
cd /home/parallels/projects/yuhan/ks-hub && go get github.com/wuhanyuhan/ks-types@v0.1.0
```

- [ ] **Step 3: 整理依赖**

```bash
go mod tidy
```

- [ ] **Step 4: 运行测试验证兼容性**

```bash
go test ./...
```

预期：全部 PASS。

- [ ] **Step 5: 提交并推送**

```bash
git add go.mod go.sum && git commit -m "chore: 切换 ks-types 到 v0.1.0 正式版本"
git push origin master
```

---

## 回滚策略

- **Task 1-4 失败**：修复代码，重新运行测试，不影响后续步骤。
- **Task 6 失败**（推送/tag 问题）：检查 git remote 配置和 SSH key。
- **Task 7-8 失败**（下游测试不过）：不提交不推送，分析失败原因。如果是 ks-types API 问题，回到 ks-types 修复 → 删除本地 tag → 重新打 tag → 发 v0.1.0（或 v0.1.1）。
